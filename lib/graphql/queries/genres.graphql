query Genres($ids: [UUID!]) {
    genres(first: 50, ids: $ids) {
        nodes {
            id
            name
            createdAt
            updatedAt
        }
        pageInfo {
            endCursor
        }
    }
}
